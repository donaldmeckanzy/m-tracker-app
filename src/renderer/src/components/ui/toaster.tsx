import React from 'react';

// Simple toaster placeholder - in a full implementation, this would be a proper toast system
export const Toaster: React.FC = () => {
  return <div id="toaster-container" className="fixed top-4 right-4 z-50" />;
};